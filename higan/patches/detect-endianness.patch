Description: Detect endianness using endian.h
 Upstream bsnes knows the endianess only for a few architectures and otherwise FTBFS.
Origin: http://anonscm.debian.org/gitweb/?p=pkg-games/bsnes.git;a=tree;f=debian/patches

Index: higan-094/nall/intrinsics.hpp
===================================================================
--- higan-094.orig/nall/intrinsics.hpp	2014-02-26 19:03:22.526227052 +0100
+++ higan-094/nall/intrinsics.hpp	2014-02-26 19:04:42.158226372 +0100
@@ -1,6 +1,8 @@
 #ifndef NALL_INTRINSICS_HPP
 #define NALL_INTRINSICS_HPP
 
+#include <endian.h>
+
 namespace nall {
 
 struct Intrinsics {
@@ -65,10 +67,10 @@
 
 /* Endian detection */
 
-#if (defined(__BYTE_ORDER) && defined(__LITTLE_ENDIAN) && __BYTE_ORDER == __LITTLE_ENDIAN) || defined(__LITTLE_ENDIAN__) || defined(__i386__) || defined(__amd64__) || defined(_M_IX86) || defined(_M_AMD64)
+#if __BYTE_ORDER == __LITTLE_ENDIAN
   #define ENDIAN_LSB
   Intrinsics::Endian Intrinsics::endian() { return Intrinsics::Endian::LSB; }
-#elif (defined(__BYTE_ORDER) && defined(__BIG_ENDIAN) && __BYTE_ORDER == __BIG_ENDIAN) || defined(__BIG_ENDIAN__) || defined(__powerpc__) || defined(_M_PPC)
+#elif __BYTE_ORDER == __BIG_ENDIAN
   #define ENDIAN_MSB
   Intrinsics::Endian Intrinsics::endian() { return Intrinsics::Endian::MSB; }
 #else
