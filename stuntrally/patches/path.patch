Also directly enter the paths. Searching them will not work.
Index: stuntrally-2.2/source/vdrift/pathmanager.cpp
===================================================================
--- stuntrally-2.2.orig/source/vdrift/pathmanager.cpp	2013-11-24 21:39:47.855080091 +0100
+++ stuntrally-2.2/source/vdrift/pathmanager.cpp	2013-11-24 21:39:47.843080092 +0100
@@ -163,6 +163,11 @@
 	CreateDir(DataUser(), error_output);  // user data
 
 
+	// Insert the correct directories for the Debian package.
+	game_data = "/usr/share/games/stuntrally/";
+	game_config = "/usr/share/games/stuntrally/config/";
+
+#if 0
 	// Find game data dir and defaults config dir
 	char *datadir = getenv("STUNTRALLY_DATA_ROOT");
 	if (datadir)
@@ -204,6 +209,7 @@
 			if (!game_data.empty() && !game_config.empty())  break;
 		}
 	}
+#endif
 
 
 	//  Subdirs for each sim_mode
Index: stuntrally-2.2/cmake/FindOGRE.cmake
===================================================================
--- stuntrally-2.2.orig/cmake/FindOGRE.cmake	2013-11-24 21:39:47.855080091 +0100
+++ stuntrally-2.2/cmake/FindOGRE.cmake	2013-11-24 21:40:37.083078627 +0100
@@ -89,7 +89,7 @@
     /opt/ogre
     /opt/OGRE
     /usr/lib${LIB_SUFFIX}/ogre
-    /usr/lib${LIB_SUFFIX}/OGRE
+    /usr/lib${LIB_SUFFIX}/OGRE1.8
     /usr/local/lib${LIB_SUFFIX}/ogre
     /usr/local/lib${LIB_SUFFIX}/OGRE
     $ENV{HOME}/ogre
