#!/usr/bin/make -f
%:
	dh $@ --parallel

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_SKIP_RPATH=ON -DCMAKE_VERBOSE_MAKEFILE=ON

override_dh_install:
	cp obj-*/AstroMenace .
	./AstroMenace --pack --rawdata=./RAW_VFS_DATA

	dh_install
